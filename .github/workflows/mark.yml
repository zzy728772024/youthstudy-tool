name: 'GitHub Actions Youthstudy Bot'
# UTC17:00(北京UTC+8 01:00点)触发任务
on:
  push:
  schedule:
    - cron: '0 17 * * *'

jobs:
  youthstudymark:
     runs-on: ubuntu-latest
     steps:
       - name: 'xecute command'
         run:                                 
          python main.py
